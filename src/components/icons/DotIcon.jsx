import React from "react";

function DotIcon({ className }) {
    return (
        <button
            className={`w-[10px] aspect-1 rounded-full ${className}`}
        ></button>
    );
}

export default DotIcon;
